import type { NextConfig } from "next";
// import type { Configuration } from 'webpack-dev-middleware';

const nextConfig: NextConfig = {
  /* config options here */
  // webpackDevMiddleware: (config: Configuration) => {
  //   config.watchOptions = {
  //     poll: 1000,
  //     aggregateTimeout: 300,
  //   }
  //   return config
  // },
};

export default nextConfig;
